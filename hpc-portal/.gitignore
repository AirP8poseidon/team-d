# 런타임 DB (멱등 재생성됨 — 커밋 금지)
app.db
app.db-wal
app.db-shm
*_test.db

# 가상환경 / 캐시
.venv/
__pycache__/
.pytest_cache/
*.pyc

# push 런타임 데이터 (폴더는 .gitkeep 로 유지)
data/incoming/*.json

demo.db*

server.out.log
server.err.log

refresh.out.log
refresh.err.log
